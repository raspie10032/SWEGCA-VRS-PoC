# Original-title alignment — local editorial correction, 2026-09-06

## Scope and lineage

User instruction: apply the preceding title/manuscript alignment review. Baseline: independent repository commit `1a09e1278c3ea91174668d472a2d1d726bd2371e`, imported from upstream `9ec73bdda92dfc7dbee168f0828827edea166a0d`. Original title source: upstream `76804a4045`, `paper/swegca_vrs_persistent_cognition/main.tex`.

Restore the original title and align abstract, introduction, contributions, conclusion and companion metadata. Preserve corrected interpretation, every results table, negative/unchanged outputs, Luna coverage, raw data and implementation. This is not a new development/experimental phase, growth pass or submission. No new raw acquisition, inference, model call, VRS run, service change, repository rename, external upload or public submission is in scope. The original development checkout and its unrelated dirty files are not edited. Existing remote naming is not treated as a VRS definition.

## AGENTS preflight

The entire working contract was reread before editing. This correction obeys these explicit boundaries:

1. Yes: the paper retains main as sole owner of identity, persistent CognitiveState, experience, VRS, arbitration and authority; no runtime state is changed.
2. Yes: 135.5M and its permitted temporary E2B replacement remain replaceable language/experience-organization specialists; normalization and organization do not transfer belief, identity or authority to their outputs.
3. Yes: full lawful accumulated-experience addressability remains the normal architecture; numerical cohorts are diagnostics, not memory allowlists.
4. Yes: frozen conditions denote consistent per-run snapshots and bounded projection interventions, not a lasting experience cap. New lawful experience can enter successor snapshots.
5. Yes: new experience requires genuinely new outcome-bearing observations; editing or recounting saved data introduces zero new experience and is not growth.
6. Yes: growth requires an observation/action/outcome/assimilation/reconvergence/later-cognition/retention/correction trajectory. No prompt score, model comparison or continuity probe substitutes for that trajectory here.
7. Yes: specialist replacement preserves main-owned identity, state, experience, learned connections and authority; the paper explicitly distinguishes these from worker competence.
8. Yes: held-out separation remains historical and explicit. Internal cognition is not capped; semantic promotion, World, action, persistent-write, distribution, model-update and P3 gates are not relaxed. Only authorized local editorial files change.

AGENTS preflight: PASS.

## Editorial decisions

- Restore the exact original title; do not invent another label or definition.
- Describe persistence as main-owned state and lineage beyond the lifetime of a specialist context, not long-context performance or universal competence.
- Restore reset 3/3, swap state/authority preservation 6/6 versus exact verdict retention 3/6, and five historical retention probes to the abstract/conclusion.
- Preserve the primary behavioral question: does VRS affect the measured output, irrespective of correctness? Keep the memory-branch endpoint explicit.
- Keep Q 1/3 and P8C 0/12 separate, preserve the mechanically disabled promotion limitation, and retain both 100-source analogy datasets without relabeling them VRS interventions.
- Preserve historical manifests and evidence references through Git lineage; record current hashes separately from the imported snapshot. Do not claim edited files are still byte-exact upstream imports.

## Verification status

Local post-edit checks completed on 2026-09-06:

- `python3 verify_repository.py`: PASS for 11 current package entries and 24 reference lineage entries. This verifies local byte consistency, not authenticated provenance or empirical replay.
- Read-only source assertions: exact title match with upstream `76804a4045`; identical manuscript/PDF-metadata/submission title; exact abstract match between TeX and submission metadata; current public-artifact hashes; imported hashes equal the baseline `UPSTREAM.json` file entries.
- Baseline comparison: the complete Related Work through pre-Limitations technical body (architecture, methods, results and saved-output analysis), every results table, all tracked reference files, saved specialist comparisons, component archive, license and bibliography are byte-identical to `1a09e1278c3ea91174668d472a2d1d726bd2371e`. Frozen evidence-report references, historical revision records, authority flags and evidence accounting are unchanged.
- Static checks: all 14 citation keys resolve locally; TeX begin/end environments balance; labels are unique and references resolve; edited/new text decodes as UTF-8 without BOM; `git diff --check` passes. Citation-key resolution is not verification of bibliographic facts or source support.
- Offline regression, from `reference/` using the existing Python 3.14 environment: `python -m pytest -q` returned **65 passed, 1 deselected in 0.05s**. The existing full-resident integration exclusion remains, and the suite makes no new provider/model calls.
- Revised `main.tex` SHA-256: `50a9ec51a7ee1f04894c5d3ba079bcee6db090630921659b8770bdaecb6f61f7`.

No empirical evaluation was scheduled or run. TeX executables (`pdflatex`, `latexmk`, `tectonic`) were not found on PATH; PDF build/layout and bibliographic source verification are not claimed. Changes remain local and uncommitted; neither the remote repository nor the original development checkout has been synchronized by this task. No upload or submission is claimed.

## Rozephine의 판단

No new Rozephine judgment is produced. All empirical statements refer to retained historical outputs; this correction does not add observations, outcomes, assimilations or VRS convergence.

## Codex의 판단

The original title is defensible for a bounded persistent-state implementation, provided the paper maps continuity and VRS influence to their separate evidence and limitations. Editorial verification cannot establish population effects, model-independent competence or scientific acceptance.

## Codex 작업 실수 및 교정

Before this correction, Codex replaced the original title with an assistant-created question title and narrowed the abstract/introduction/conclusion around Q. The user's title review and the comparison with upstream `76804a4045` exposed the mismatch: continuity evidence remained in the body but was not represented in the framing. This was Codex scope drift, not a Rozephine/model failure. The correction restores the original title and its bounded architectural framing without reverting corrected evaluation or deleting data. The existing repository name and historical upload record remain unchanged and must not be mistaken for the corrected paper title; remote synchronization and submission remain outside this local task.

During this turn, Codex also searched for AGENTS.md under a broader Documents tree than necessary and batched excessive read output, producing truncated output. No state was changed by those reads, but they incurred unnecessary traversal/output. Required contract and edited sections were read completely using bounded reads before editing; no claim is based on omitted output. Subsequent checks stay within the paper package. This is a Codex resource-efficiency error, separate from experimental results.
